#include <stdio.h>
#include <math.h>

int main(){

    double A  = sqrt(9); //lf
    double B = pow(2, 4); //lf
    int C = round(3.14); // d
    int D = ceil(4.14); // d
    int E = floor(3.99); //d

    printf("\n%d\n", C);

    return 0;
}